services:
  backend:
    image:  ghcr.io/diwiatschool/seminar-2025
#    command: ["sleep", "900"]
    ports:
      - 8080:8080
    environment:
      - quarkus.datasource.jdbc.url=jdbc:postgresql://postgres:5432/demo
      - PUBLIC_KEY=ybc
    depends_on:
      postgres:
        condition: service_healthy